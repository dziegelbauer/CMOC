@page
@model CMOC.Pages.Admin.Locations.Upsert

<form method="post" enctype="multipart/form-data">
    <input asp-for="Location.Id" hidden/>
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <div class="col-9">
                <h2 class="text-primary pl-3">
                    @(Model.Location.Id == 0 ? "Create" : "Update") Location
                </h2>
                <hr/>

                <div class="mb-3 form-floating">
                    <label asp-for="Location.Name"></label>
                    <input type="text" asp-for="@Model.Location.Name" class="form-control"/>
                    <span asp-validation-for="Location.Name" class="text-danger"></span>
                </div>
            </div>
        </div>
        @if (Model.Location.Id == 0)
        {
            <button type="submit" class="btn btn-primary" style="width: 150px;">Create</button>
        }
        else
        {
            <button type="submit" class="btn btn-primary" style="width: 150px;">Update</button>
        }
        <a asp-page="/Admin/Locations/Index" class="btn btn-secondary" style="width: 150px;">Back to List</a>
    </div>
</form>